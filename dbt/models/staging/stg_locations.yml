models:
  - name: stg_locations
    description: List of open locations with basic cleaning and transformation applied, one row per location.
    columns:
      - name: location_id
        description: The unique identifier for each location. Used as the primary key of the locations table.
        tests:
          - not_null
          - unique
